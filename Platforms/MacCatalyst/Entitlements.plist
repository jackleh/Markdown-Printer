<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!-- Required so the sandbox grants read/write access to the folder the user
             picks in the Save document picker when exporting markdown and HTML. -->
        <key>com.apple.security.files.user-selected.read-write</key>
        <true/>
        <!-- Required so the sandboxed app can reach the printing system. Without it
             macOS reports that the application does not support printing. -->
        <key>com.apple.security.print</key>
        <true/>
    </dict>
</plist>

